[build]
builder = "nixpacks"

[deploy]
preDeployCommand = "python manage.py collectstatic --noinput; python manage.py migrate; python manage.py seed"
startCommand = "/bin/sh -c \"gunicorn config.wsgi:application --bind 0.0.0.0:${PORT}\""
healthcheckPath = "/health/"
healthcheckTimeout = 30
